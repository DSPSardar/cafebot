{
  "name": "backend",
  "version": "1.0.0",
  "description": "The API / server logic that connects the frontend to the LLM and handles CafeBot's responses.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.119.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
