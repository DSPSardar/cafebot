# Copy this file to .env and fill in your own values.
# Never commit your actual .env file — it's already ignored in .gitignore.

# API key for the LLM provider CafeBot will use (Anthropic)
ANTHROPIC_API_KEY=your-api-key-here

# Port the backend server will run on
PORT=3000
